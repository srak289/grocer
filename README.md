Grocer App
---

To build:

```bash
    make
```

To make the testfile:

```bash
    make tests
```

To load the database:

```bash
    ./main -f grocer.txt
```

To run the app:

```bash
    ./main
```
